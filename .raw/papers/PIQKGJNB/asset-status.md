# Asset status: PIQKGJNB

Snapshot at ingest time (2026-05-11).

| Asset | Status |
|---|---|
| Zotero PDF attachment | present (`MYQEQR3K`) |
| Zotero SI attachment | missing (publisher SI exists; not yet downloaded into Zotero) |
| Source Data (Zotero) | missing (publisher Source Data exists per DA statement; not yet downloaded) |
| Data Availability | extracted (see `data-availability.md`) |
| Code Availability | extracted (see `code-availability.md`) |
| External repo links | not found (no Zenodo / Figshare / Dryad / GitHub / OSF / Dataverse) |
| Defuddled landing page | deferred (see `article-page.md`) |

## What to do next on this paper

- If a pattern page (`patterns/data-availability/*` or `patterns/code-availability/*`) ever needs this paper as a strong example or counter-example, download the publisher SI + Source Data into the Zotero parent item (do NOT copy into this iCloud repo).
- If a figure-pattern synthesis (`patterns/figures/*`) needs the published figure captions verbatim, run defuddle on the landing page.
